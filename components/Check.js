
/**
 * 
 * @param {*} isChecked : 체크되어있다면, 객체를 추가. 아니면 객체에서 삭제 아니면 그냥 두기
 *
 */
export const toggleCheckbox = (isChecked) => {
  let cnames = [];
  let jibuns = [];
  let buyeraddrs = [];
  let areas = [];

  if (isChecked){
    
  }
}

export const pushItemTo = (obj, state) => {

}